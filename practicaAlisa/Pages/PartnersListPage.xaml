<Page x:Class="practicaAlisa.Pages.PartnersListPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
      mc:Ignorable="d" 
      d:DesignHeight="600" d:DesignWidth="1000"
      Title="Партнеры - Комфорт"
      Background="White">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="4*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <Border Grid.Row="0" Background="#D2DFFF" Padding="10" Margin="10">
            <StackPanel Orientation="Horizontal">
                <TextBlock Text="Поиск:" VerticalAlignment="Center" FontSize="14" FontWeight="Bold"/>
                <TextBox x:Name="SearchBox" Width="200" Height="30" TextChanged="SearchBox_TextChanged" Margin="5,0"/>
                <TextBlock Text="Сортировка:" VerticalAlignment="Center" FontSize="14" FontWeight="Bold" Margin="20,0,0,0"/>
                <ComboBox x:Name="SortComboBox" Width="150" Height="30" SelectionChanged="SortComboBox_SelectionChanged" Margin="5,0">
                    <ComboBoxItem Content="По имени (А-Я)"/>
                    <ComboBoxItem Content="По имени (Я-А)"/>
                    <ComboBoxItem Content="По рейтингу ↑"/>
                    <ComboBoxItem Content="По рейтингу ↓"/>
                </ComboBox>
            </StackPanel>
        </Border>

        <ListView x:Name="PartnersLW" Grid.Row="1" FontSize="20">
            <ListView.ItemTemplate>
                <DataTemplate>
                    <Border BorderBrush="Black"
                        BorderThickness="1" Margin="10">
                        <Grid Margin="10" Width="700">
                            <Grid.RowDefinitions>
                                <RowDefinition/>
                                <RowDefinition/>
                                <RowDefinition/>
                                <RowDefinition/>
                            </Grid.RowDefinitions>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition/>
                                <ColumnDefinition/>
                            </Grid.ColumnDefinitions>
                            <StackPanel Orientation="Horizontal">
                                <TextBlock Text="{Binding TypeOfBusiness.NameBusiness}"/>
                                <TextBlock Text=" | "/>
                                <TextBlock Text="{Binding NamePartner}"/>

                            </StackPanel>


                            <StackPanel Grid.Row="1" Orientation="Horizontal">
                                <TextBlock Text="{Binding SurnameDirector}"/>
                                <TextBlock Text="{Binding NameDirector}"/>
                                <TextBlock Text="{Binding PatronumicDirector}"/>
                            </StackPanel>
                            <TextBlock Grid.Row="2" Text="{Binding Phone}"/>
                            <TextBlock Grid.Row="3" Text="{Binding Raiting}"/>
                            <TextBlock Grid.Row="0" Grid.Column="1" HorizontalAlignment="Right" Text="{Binding SizeDiscount}"/>
                        </Grid>
                    </Border>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>

        <StackPanel Orientation="Horizontal" Grid.Row="2" HorizontalAlignment="Center" Margin="10">
            <Button Click="AddBtn_Click" Background="#355CBD" x:Name="AddBtn" Width="100" Height="35" Margin="5">Добавить</Button>
            <Button Click="EditBtn_Click" Background="#355CBD" x:Name="EditBtn" Width="100" Height="35" Margin="5">Изменить</Button>
            <Button Click="DeleteBtn_Click" x:Name="DeleteBtn" Width="100" Height="35" Margin="5" Background="#FF5555">Удалить</Button>
            <Button Click="HistoryBtn_Click" Background="#355CBD" x:Name="HistoryBtn" Width="120" Height="35" Margin="5">История продаж</Button>
            <Button Click="LogoutBtn_Click" Background="#355CBD" x:Name="LogoutBtn" Width="100" Height="35" Margin="5">Выход</Button>
        </StackPanel>

        <TextBlock Grid.Row="2" x:Name="StatusText" Text="Готово" FontSize="10" Margin="10" HorizontalAlignment="Left" VerticalAlignment="Bottom"/>
    </Grid>
</Page>