<Page x:Class="practicaAlisa.Pages.SaleHistoryPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
      mc:Ignorable="d" 
      d:DesignHeight="600" d:DesignWidth="1000"
      Title="История продаж - Комфорт"
      Background="White">

    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="История реализации продукции" 
                   FontSize="28" FontWeight="Bold" 
                   HorizontalAlignment="Center" Margin="0,0,0,10"
                   Foreground="#355CBD"/>

        <Border Grid.Row="1" Background="#D2DFFF" Padding="15" Margin="0,0,0,20" CornerRadius="5">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <StackPanel>
                    <TextBlock x:Name="PartnerNameText" FontSize="18" FontWeight="Bold" 
                               Foreground="#355CBD"/>
                    <TextBlock x:Name="TotalSalesText" FontSize="14" Margin="0,5,0,0"/>
                </StackPanel>

                <Border Grid.Column="1" Background="#355CBD" CornerRadius="10" 
                        Padding="15,8" VerticalAlignment="Center">
                    <TextBlock x:Name="DiscountText" Foreground="White" 
                               FontSize="16" FontWeight="Bold"/>
                </Border>
            </Grid>
        </Border>

        <DataGrid x:Name="SalesGrid" Grid.Row="2" 
                  AutoGenerateColumns="False" 
                  CanUserAddRows="False"
                  CanUserDeleteRows="False"
                  IsReadOnly="True"
                  AlternatingRowBackground="#D2DFFF"
                  FontSize="12"
                  Margin="0,0,0,10">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Дата продажи" Binding="{Binding SaleDate, StringFormat='dd.MM.yyyy'}" 
                                    Width="120"/>
                <DataGridTextColumn Header="Наименование продукции" Binding="{Binding ProductName}" 
                                    Width="350"/>
                <DataGridTextColumn Header="Количество" Binding="{Binding Quantity}" 
                                    Width="100"/>
                <DataGridTextColumn Header="Сумма" Binding="{Binding Amount, StringFormat='N2'}" 
                                    Width="150"/>
                <DataGridTextColumn Header="Точка продаж" Binding="{Binding PointName}" 
                                    Width="200"/>
            </DataGrid.Columns>
        </DataGrid>

        <Button Grid.Row="3" Content="← Назад" Width="150" Height="40" FontSize="14"
                Background="#355CBD" Foreground="White"
                Click="Back_Click" HorizontalAlignment="Center"/>
    </Grid>
</Page>